import './Header.css';

const Header = () => {
    return (
        <header className="header">
            <h1 className="header-title">Proof of Security</h1>
        </header>
    );
}

export default Header;
